apiVersion: v1
kind: Service
metadata:
  name: django-school-management-system
spec:
  type: NodePort
  selector:
    app: django-school-management-system
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30083
